{
  "name": "paperback-corruptbytes",
  "private": true,
  "description": "High-care Paperback sources for reliable catalogs, search, and legitimately accessible chapters.",
  "homepage": "https://michaelasper.github.io/paperback-corruptbytes/",
  "bugs": {
    "url": "https://github.com/michaelasper/paperback-corruptbytes/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/michaelasper/paperback-corruptbytes.git"
  },
  "type": "module",
  "scripts": {
    "tsc": "tsc",
    "lint": "oxlint --type-aware --fix",
    "lint:check": "oxlint --type-aware --deny-warnings",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "conformance": "npm run tsc && npm run lint:check && npm run format:check && npm run verify:versions",
    "bundle": "paperback-cli bundle && npm run verify:bundles",
    "verify:bundles": "node scripts/verify-bundles.mjs",
    "verify:published": "node scripts/verify-published-repository.mjs",
    "verify:versions": "node scripts/verify-version-bumps.mjs",
    "serve": "paperback-cli serve",
    "dev": "paperback-cli serve --watch",
    "logcat": "paperback-cli logcat",
    "test": "tsx --test src/*/*.test.ts scripts/*.test.mjs",
    "test:live": "VORTEX_LIVE_TESTS=1 VALIR_LIVE_TESTS=1 THUNDER_LIVE_TESTS=1 MGEKO_LIVE_TESTS=1 MADARADEX_LIVE_TESTS=1 DIVA_LIVE_TESTS=1 ATSUMARU_LIVE_TESTS=1 tsx --test src/*/live.test.ts",
    "test:live:github": "VORTEX_LIVE_TESTS=1 VALIR_LIVE_TESTS=1 THUNDER_LIVE_TESTS=1 MGEKO_LIVE_TESTS=1 DIVA_LIVE_TESTS=1 ATSUMARU_LIVE_TESTS=1 tsx --test src/*/live.test.ts",
    "test:live:atsumaru": "ATSUMARU_LIVE_TESTS=1 tsx --test src/Atsumaru/live.test.ts",
    "test:live:diva": "DIVA_LIVE_TESTS=1 tsx --test src/DivaScans/live.test.ts",
    "test:live:madaradex": "MADARADEX_LIVE_TESTS=1 tsx --test src/MadaraDex/live.test.ts",
    "test:live:mgeko": "MGEKO_LIVE_TESTS=1 tsx --test src/Mgeko/live.test.ts",
    "test:live:thunder": "THUNDER_LIVE_TESTS=1 tsx --test src/Thunderscans/live.test.ts",
    "test:live:valir": "VALIR_LIVE_TESTS=1 tsx --test src/ValirScans/live.test.ts",
    "test:live:random": "tsx scripts/random-live.ts",
    "test:parsers": "tsx --test src/*/parsers.test.ts"
  },
  "devDependencies": {
    "@paperback/toolchain": "^1.0.0-alpha.92",
    "@paperback/types": "^1.0.0-alpha.92",
    "cheerio": "^1.2.0",
    "oxfmt": "^0.61.0",
    "oxlint": "^1.76.0",
    "oxlint-tsgolint": "^7.0.2001",
    "rolldown": "^1.2.3",
    "tsx": "^4.20.6",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=24"
  }
}
